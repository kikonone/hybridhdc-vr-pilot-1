# Phase 04B — Traditional Regression Baselines

This phase is initialized for frozen-input preflight only. It reuses the Phase 03 primary without-performance dataset and frozen subject-wise outer-fold assignments through absolute, read-only paths. No regression model has been instantiated, fitted, tuned, or evaluated.

The executed notebook and audit JSON record the required data, fold, and persistence checks. The phase status remains `INITIALIZED / NOT YET MODELED`.
